# 投稿ポリシー（頻度とトピック選定）

自動執筆エージェントは **執筆前に必ずこのファイルを読む**。
`TOPIC_BACKLOG.md` の選定手順はこのファイルに従う。

---

## 1. 投稿頻度

### 上限（アカウント凍結・spam 判定の回避）

| ルール | 値 |
|---|---|
| Zenn 投稿間隔 | **週 1 本**（基本）。最大でも週 2 本 |
| 最短間隔 | **中 3 日以上空ける**（同日 2 本は禁止） |
| 1 回の実行で公開する本数 | **1 本まで**（バックログ補充直後のまとめ出し禁止） |
| Zenn と Qiita の同日投稿 | **禁止**（媒体をまたぐ場合も 1 日 1 本） |

**根拠**: このリポジトリで実際に起きた事故は 2026-06-07 の Qiita spam 判定（`supabase-cli-db-push-migration-flow` が非公開化 + PATCH が WAF 403）。
原因は投稿頻度ではなく **記事末尾の自社プロダクト宣伝リンクの羅列** だった。`scripts/validate-articles.mjs` の `checkPromoFooter` が現在これを CRITICAL で弾いている。

つまり凍結リスクの主因は頻度より**内容**である。週 1 は十分に安全側で、5〜6 月に 12 本を週 1 で出して問題は起きていない。頻度を上げる動機がない限り週 1 を維持する。

> 注: Zenn / Qiita の利用規約ページはこの実行環境から取得できない（egress ブロック）。
> 上の数値は規約の引用ではなく、本リポジトリの事故履歴と実績から決めた保守的な運用値。

### 下限（枯渇による停止の防止）

**実際に起きた障害は「出しすぎ」ではなく「止まったこと」**。
2026-06-29 → 2026-08-19 の **51 日間**、バックログ枯渇で無投稿だった。

これを再発させないため、毎回の実行で残スロットを数え、以下を**通知に必ず含める**。

| Zenn 未執筆スロット数 | 対応 |
|---|---|
| 4 本以上 | 通常運転 |
| **3 本以下** | 通知に「⚠️ 補充推奨（残 N 本）」を明記 |
| **0 本** | 記事は書かず、「🚨 ネタ切れ・補充が必要」を通知して終了 |

残数は次で数える（Zenn セクションのみ）:

```bash
ls articles/ articles/_drafts/ | sed 's/\.md$//' | sort -u > /tmp/have.txt
# TOPIC_BACKLOG.md の「Zenn 用」表の slug のうち /tmp/have.txt に無いものが残スロット
```

---

## 2. トピック選定（ホットな題材の分析）

### 2-1. 使える情報源（2026-09-05 実測）

この環境は egress が絞られている。**到達可能なのは実質 github.com だけ**。

| 情報源 | WebFetch | 用途 |
|---|---|---|
| `github.com/trending` | ✅ | 今ホットな OSS |
| `github.com/<org>/<repo>/releases` | ✅ | 破壊的変更・新機能（**最重要**） |
| `github.com/<org>/<repo>/issues?q=sort:comments-desc` | ✅ | 人が実際にハマっている箇所 |
| zenn.dev / qiita.com | ❌ | ブロック。トレンド直接取得は不可 |
| news.ycombinator.com | ❌ | ブロック |
| nextjs.org / supabase.com / w3.org 等の公式ドキュメント | ❌ | ブロック |

**ブロック済みホストに WebFetch を試して時間を溶かさないこと。**
公式ドキュメントの一次ソースが要る場合は、GitHub 上の release notes / CHANGELOG / repo 内 docs を引用元にする。

### 2-2. スコアリング（満点 10 点、7 点以上で採用）

| 軸 | 配点 | 基準 |
|---|---|---|
| **一次体験** | **4** | 自分が実際に踏んだ / 運用している領域か |
| 検索需要 | 3 | 破壊的変更・移行・エラー文言など「調べる理由」があるか |
| 鮮度 | 3 | 直近リリース・直近で議論が立っているか |

**一次体験が 0 点のトピックは、他が満点でも却下する。**

これは飾りではない。`REVIEW_CHECKLIST.md` の人間味 15 点は一次体験と失敗開示に依存しており、触ったことのない題材では 100/100 に到達できない。
`TOPIC_BACKLOG.md` の「保留」欄が同じ原則で運用されている（未解決の問題の対策記事は書かない）。

**ホットさだけで選ばない。ホット ∩ 自分が触った領域 の交差だけを書く。**

### 2-3. 最も強いパターン

> **自分が既に書いた記事の前提を壊す変更**

例: `nextjs-env-var-genre-config-pattern` を書いた後に Next.js 16 が `publicRuntimeConfig` を削除した。
このとき「移行してどうハマったか」は、鮮度・検索需要・一次体験の 3 拍子が揃う。

実行時はまず **`articles/` の既存 slug を読み、その前提を壊す変更が最近のリリースに無いか** を探す。

---

## 3. 実行フロー（Step 2 の差し替え）

1. `articles/` `articles/_drafts/` の既存 slug を列挙
2. Zenn 未執筆スロットを数える → **0 なら書かずに通知して終了**
3. `TOPIC_BACKLOG.md` の Zenn セクションから候補を取る
4. 候補を 2-2 でスコアし、7 点未満しか無ければ 2-1 の情報源でホット題材を再収集して補充
5. 最高点の 1 本を選ぶ（同点なら既存記事の前提を壊すものを優先）
6. 以降は既存の執筆 → 検証 → レビュー → 公開フローに従う
